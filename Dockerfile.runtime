# Runtime-only image: jar is built on the host (host has working egress),
# this stage only packages it — no network RUN steps, base image is cached.
FROM eclipse-temurin:21-jre

WORKDIR /app

COPY target/*.jar app.jar

EXPOSE 8081

ENTRYPOINT ["java", "-jar", "app.jar"]
